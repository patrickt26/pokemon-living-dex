import { NavLink } from 'react-router-dom'; import { pokemonDataSource } from '../data/PokemonDataSource'; import { GameBadge } from '../components/GameBadge';
export function GamesPage(){return <><div className="page-head"><div><span className="eyebrow">CONNECTED DEXES</span><h1>Game Dexes</h1><p>Adding a Pokémon here also counts toward National progress.</p></div></div><div className="game-cards">{pokemonDataSource.getGames().filter(g=>g.id!=='home'&&g.hasDex!==false).map(g=><NavLink to={`/games/${g.id}`} key={g.id}><GameBadge game={g}/><h2>{g.name}</h2><p>{g.dexSpeciesIds.length} unique species · {g.dexSections?.map(section=>section.name).join(' + ')}</p><span>Open dex →</span></NavLink>)}</div></>}

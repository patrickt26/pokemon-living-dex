import { NavLink } from 'react-router-dom'; import { pokemonDataSource } from '../data/PokemonDataSource'; import { GameBadge } from '../components/GameBadge'; import { useI18n } from '../i18n';
export function GamesPage(){const {t}=useI18n();return <><div className="page-head"><div><span className="eyebrow">{t('connectedDexes','CONNECTED DEXES')}</span><h1>{t('gameDexes','Game Dexes')}</h1><p>{t('gamesDescription','Adding a Pokémon here also counts toward National progress.')}</p></div></div><div className="game-cards">{pokemonDataSource.getGames().filter(g=>g.id!=='home'&&g.hasDex!==false).map(g=><NavLink to={`/games/${g.id}`} key={g.id}><GameBadge game={g}/><h2>{g.name}</h2><p>{g.dexSpeciesIds.length} {t('uniqueSpecies','unique species')} · {g.dexSections?.map(section=>section.name).join(' + ')}</p><span>{t('openDex','Open dex')} →</span></NavLink>)}</div></>}

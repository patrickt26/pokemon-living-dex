import { Navigate, Route, Routes, useParams } from 'react-router-dom'; import { AppShell } from './components/AppShell'; import { pokemonDataSource } from './data/PokemonDataSource'; import { BackupPage } from './pages/BackupPage'; import { Dashboard } from './pages/Dashboard'; import { DexPage } from './pages/DexPage'; import { GamesPage } from './pages/GamesPage'; import { RegionalPage } from './pages/RegionalPage'; import { SpecialFormsPage } from './pages/SpecialFormsPage';
function GameDex(){const {gameId}=useParams();const game=pokemonDataSource.getGames().find(candidate=>candidate.id===gameId);return !game||game.hasDex===false?<Navigate to="/games" replace/>:<DexPage key={gameId} gameId={gameId}/>}
export default function App(){return <Routes><Route element={<AppShell/>}><Route index element={<Dashboard/>}/><Route path="national" element={<DexPage/>}/><Route path="shiny" element={<Navigate to="/national" replace/>}/><Route path="regional" element={<RegionalPage/>}/><Route path="forms" element={<SpecialFormsPage/>}/><Route path="games" element={<GamesPage/>}/><Route path="games/:gameId" element={<GameDex/>}/><Route path="backup" element={<BackupPage/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Route></Routes>}

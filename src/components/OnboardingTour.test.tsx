import { fireEvent,render,screen } from '@testing-library/react';import { beforeEach,describe,expect,it } from 'vitest';import { OnboardingTour } from './OnboardingTour';
describe('OnboardingTour',()=>{beforeEach(()=>localStorage.clear());it('shows on first visit and remembers completion',()=>{const {rerender}=render(<OnboardingTour/>);expect(screen.getByRole('dialog')).toBeInTheDocument();expect(screen.getByText('Organize your Living Dex')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Next'}));expect(screen.getByText('Update with a double click')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Skip tutorial'}));expect(screen.queryByRole('dialog')).not.toBeInTheDocument();rerender(<OnboardingTour/>);expect(screen.queryByRole('dialog')).not.toBeInTheDocument()})});

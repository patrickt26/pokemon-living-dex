import { describe,expect,it } from 'vitest';import { paginateDexGroups } from './dexView';
const species=(number:number)=>({id:`species-${number}`,nationalDexNumber:number,name:`Poke ${number}`,defaultFormId:`form-${number}`});const form=(number:number)=>({id:`form-${number}`,speciesId:`species-${number}`,name:'Standard',sprite:'sprite',types:['normal'] as const});
describe('Dex view pagination',()=>{it('creates 30-slot boxes without changing dex order',()=>{const items=Array.from({length:31},(_,index)=>({species:species(index+1),form:form(index+1),dexNumber:index+1}));const boxes=paginateDexGroups({id:'national',label:null,items});expect(boxes).toHaveLength(2);expect(boxes[0]?.items).toHaveLength(30);expect(boxes[1]?.items[0]?.dexNumber).toBe(31)})});
